# Electric Link 仕様・生成ロジック資料

> 対象バージョン: **Ver.1.4.85**\
> 対象ファイル: `electric_link_v1_4_85_generation_core_rebuild.html`\
> 本資料は、現在のHTML実装を基準に、ゲーム機能・ステージ生成・難易度・ギミック・進行管理・デバッグ仕様を整理したものです。

------------------------------------------------------------------------

## 1. ゲーム概要

**Electric Link**
は、盤面上のケーブルブロックを90°ずつ回転させ、電源から番号ライトを順番に通電し、最後にゴールへ電気を届ける回路パズルです。

基本目標は次のとおりです。

1.  電源から正しい回路を構築する。
2.  番号ライトを **1 → 2 → 3 → ...** の順番で成立させる。
3.  同じ番号のライトが複数ある場合は、その番号のライトをすべて成立させる。
4.  すべての必須ライトが点灯した状態でゴールへ通電する。
5.  ステージをクリアして次のステージを解放する。

全100ステージで構成され、100ステージクリア後は盤面を再生成して次の周回へ進む設計です。

------------------------------------------------------------------------

## 2. 基本操作

### ケーブル操作

-   回転可能なブロックを選択・タップすると90°回転する。
-   固定ブロックは回転できない。
-   電源・ゴールなど、ゲーム上回転させない部品も存在する。
-   初期回転はステージシードから決定論的に決めるため、同じ周回・同じステージでは基本的に同じ初期状態になる。

### ステージ選択

-   ステージは10個単位でページ表示される。
-   `◀ / ▶` で前後10ステージへ移動する。
-   未解放ステージは選択できない。
-   クリア済みステージには専用表示が付く。

------------------------------------------------------------------------

## 3. 通電とクリア条件

### 3.1 物理的な接続判定

回路の物理接続は電源を起点に探索されます。

隣接するブロック同士で、

-   現在ブロックが隣方向へのポートを持つ
-   隣ブロックが反対方向へのポートを持つ
-   一方通行などの特殊条件を満たす

場合に電気が流れます。

### 3.2 ライトの順番ゲート

物理的につながっているだけでは、後続ライトは点灯しません。

現在未完成の最小番号を `activeOrder`
とし、それより後の番号への表示上の通電を制限します。

例:

``` text
① 未完成 → ②・③には進めない
① 完成   → ②を攻略可能
② 完成   → ③を攻略可能
```

### 3.3 同番号ライト

同じ番号が複数存在する場合、そのグループを1セットとして扱います。

例:

``` text
       ┌─ ②
① ─ T ┤
       └─ ②

②が両方成立
    ↓
③へ進行可能
```

片方の②だけでは③へ進めません。

### 3.4 ライト充電

ライトは接続した瞬間ではなく、一定時間通電すると点灯します。

現在実装では充電時間の基準は約 **520ms** です。

前段の接続が崩れた場合、後続番号は再ロックされ、必要に応じて消灯・充電リセットされます。

### 3.5 クリア条件

次の両方を満たした場合にクリアです。

-   必須ライトがすべて点灯済み
-   ゴールが現在の有効通電ネットワークに接続されている

------------------------------------------------------------------------

## 4. ブロック・ギミック仕様

### 4.1 電源

-   回路の開始地点。
-   1ポート。
-   回転不可。
-   ここから通電探索を開始する。

### 4.2 ゴール

-   回路の最終地点。
-   1ポート。
-   必須ライトをすべて成立させたあとに通電する必要がある。

### 4.3 通常ケーブル

基本ケーブルは以下を使用します。

-   ストレート
-   L字

通常ケーブルは原則 **2ポート** です。

`・`
のような0ポートブロックは正常なステージ部品として生成しない方針です。

### 4.4 番号ライト

-   必須ライトは2ポート。
-   本線上では進行順に番号を割り当てる。
-   番号は生成時に確定し、生成後に番号を振り直さない。
-   同番号ライトが複数ある場合は全灯が必要。

### 4.5 T字分岐

T字は3ポートの正解回路部品です。

現在の設計では、T字は単なる分岐ではなく
**同番号ライトのペア構造**として扱います。

基本形:

``` text
              ┌─ ケーブル ─ ②
① ─ T字 ─────┤
              └─ ケーブル ─ ②
```

ルール:

-   T字の両枝で対象となるライト番号を一致させる。
-   `T字 → ② / ③` のように異なる番号へ直接分岐する構造は禁止。
-   同番号ライトが両方成立してから次番号へ進む。
-   ダミー側の末端も0ポート・1ポートにはしない。
-   ダミー末端はストレートまたはL字の2ポートとして表示する。
-   品質を満たせない場所には、無理にT字を配置しない。

### 4.6 立体交差

立体交差は、縦軸と横軸が中央で電気的に接続せず交差する特殊ブロックです。

ルール:

-   4ポートで表現する。
-   画面端には生成しない。
-   本線側が直線であることを要求する。
-   もう一方の軸にも実際の配線を配置できる場合のみ採用する。
-   「立体交差ブロックだけ存在するが片軸しか使わない」という配置は避ける設計。
-   通電表示は縦軸・横軸を別々に管理する。

### 4.7 一方通行

-   2ポート。
-   指定された方向にのみ通電できる。
-   現在のクリーンジェネレータでは、本線上のストレート部分を候補とする。
-   逆方向からの通電は禁止。

### 4.8 固定ブロック

-   回転不可。
-   🔒などで固定属性を表示する。
-   固定ブロックが多すぎると正解ルートのヒントになりすぎるため、難易度が高くても少量に抑える。

------------------------------------------------------------------------

## 5. Ver.1.4.85 ステージ生成の基本思想

Ver.1.4.85では、過去の「生成後に問題を補正する」方式から、**生成時点で正しい構造を作る方式**へ整理しています。

基本フロー:

``` text
LEVEL・周回からシード決定
        ↓
盤面サイズ決定
        ↓
難易度ルール決定
        ↓
T字・立体交差・一方通行の構成案決定
        ↓
正解本線ルート生成
        ↓
ルート品質検査
        ↓
番号ライト配置
        ↓
T字ペア生成
        ↓
立体交差生成
        ↓
一方通行生成
        ↓
正解ネットワーク確定
        ↓
残りセルへダミー配線配置
        ↓
固定ブロック配置
        ↓
完成盤面監査
        ↓
初期回転
        ↓
プレイ開始
```

重要なのは、ライト番号・T字関係などを後から無理に修正しないことです。

------------------------------------------------------------------------

## 6. シードと再現性

### 6.1 ステージシード

100ステージそれぞれに基礎シードがあります。

現在は `STAGE_REBUILD_EPOCH = 178` を基準に、100個の
`STAGE_LAYOUT_SALTS` を決定論的に生成しています。

### 6.2 周回

実際の生成シードは概念的に以下です。

``` text
基礎ステージシード
+ generationCycle × 1,000,003
```

そのため、

-   同じステージ番号
-   同じ周回数

であれば、基本的に同じ盤面を再現できます。

周回が変わると `generationCycle`
が変わるため、同じLEVELでも別盤面になります。

------------------------------------------------------------------------

## 7. 正解ルート生成

### 7.1 経路候補

`makeSerpentinePath()`
により、曲がり・折り返しを含む本線候補を生成します。

### 7.2 クリーン経路検査

Ver.1.4.85では生成した経路をそのまま信用せず、以下を検査します。

-   配列として成立している
-   必要な長さがある
-   盤面外へ出ていない
-   同じセルを重複して通らない
-   連続セルが必ず上下左右で隣接している

最低経路長は概念的に、

``` text
max(14, ライト数 × 5 + 5)
```

です。

### 7.3 経路再試行

最大24候補を試します。

候補シードは段階的に変化させます。

それでも品質条件を満たすルートがない場合は、盤面を蛇行する安全な
`safeWeavePath()` を使用します。

------------------------------------------------------------------------

## 8. ライト生成ロジック

ライト番号は本線の進行方向に沿って配置します。

原則:

``` text
START
 ↓
①
 ↓
②
 ↓
③
 ↓
…
 ↓
GOAL
```

各ライト間には必要な間隔を確保します。

重要な仕様:

-   番号は最初の配置時点で確定する。
-   後処理で番号を並べ替えない。
-   `1 → 3` のような欠番を正常状態として採用しない。
-   監査時にユニーク番号列が `1,2,3,...` になっているか確認する。

------------------------------------------------------------------------

## 9. T字生成ロジック

T字は難易度設定で指定された範囲を「目標」としますが、盤面品質を優先します。

生成概要:

1.  対象番号を決める。
2.  前番号ライトと対象番号ライトの間からT字候補位置を探す。
3.  本線の進行方向と重ならない方向へ枝を作る。
4.  枝にケーブルを配置する。
5.  枝側に対象番号と同じライトを配置する。
6.  さらにダミー末端を確保する。
7.  盤面外・本線・既存枝との衝突を確認する。
8.  成立する場合だけ採用する。

つまり「T字をN個必ず入れる」のではなく、

> **指定範囲を狙うが、品質条件を満たしたT字だけ採用する**

という考え方です。

------------------------------------------------------------------------

## 10. 立体交差生成ロジック

候補条件:

-   本線上のセルである。
-   START/GOAL付近など不適切な位置ではない。
-   ライトなど別部品が置かれていない。
-   盤面端ではない。
-   本線がそのセルを直線で通過する。
-   直交するもう一方の軸に配線スペースがある。
-   T字枝など既存ネットワークと衝突しない。

成立した場合のみ `bridge` として採用します。

これにより「置いてあるだけの立体交差」ではなく、両軸を持つ交差構造を目標としています。

------------------------------------------------------------------------

## 11. 一方通行生成ロジック

候補は本線上の通常セルから選びます。

現在は特に、

-   2ポート
-   ストレート
-   他の特殊部品と重複しない

セルを候補とします。

進行方向から `input` と `forward` を決めます。

------------------------------------------------------------------------

## 12. ダミー配線

正解ネットワーク以外の空きセルは、ダミー配線で埋めます。

使用形状:

``` text
縦ストレート
横ストレート
L字 × 4方向
```

つまり通常のダミーセルも **必ず2ポート** です。

0ポートの `・` をダミーとして生成する設計にはしていません。

------------------------------------------------------------------------

## 13. 完成盤面の品質監査

生成後、プレイ開始前に盤面構造を監査します。

主なポート数:

  部品              正常ポート数
  -------------- ---------------
  通常ケーブル     2（T字のみ3）
  ライト                       2
  一方通行                     2
  立体交差                     4
  電源                         1
  ゴール                       1

さらに、

-   ライト番号が連番か
-   T字枝ライトと本線ライトが同番号か
-   必須ライトが物理的に到達可能か
-   ゴールへ到達可能か

などを監査します。

デバッグ用に直近の監査情報を `window.__lastGenerationAudit`
に保持します。

例として以下の情報を持ちます。

``` text
LEVEL
issues
T字数
立体交差数
一方通行数
ライト番号列
必須ライト数
正解経路長
使用経路シード
```

------------------------------------------------------------------------

## 14. 初期回転ロジック

盤面生成後、回転可能なケーブル・一方通行をシードに基づいて回転します。

-   回転量は決定論的。
-   同一シードなら同じ初期回転になる。
-   固定ブロックは回転しない。
-   最初から完全クリア状態になった場合は、正解本線の通常線を追加で1回回転して完成状態を崩す。

------------------------------------------------------------------------

## 15. 盤面サイズ

LEVELが上がるほど盤面サイズが増える設計です。

  LEVEL     列 × 行
  --------- ---------
  1--10     6 × 8
  11--20    7 × 8
  21--30    7 × 9
  31--40    7 × 10
  41--50    7 × 11
  51--60    8 × 11
  61--70    8 × 12
  71--80    8 × 12
  81--90    8 × 12
  91--100   8 × 12

途中で盤面が小さく戻らないようにしています。

------------------------------------------------------------------------

## 16. 難易度設計

100ステージを約10ステージごとの10帯に分けています。

プレイヤー向け表示は以下の5段階です。

-   やさしい
-   ふつう
-   むずかしい
-   とてもむずかしい
-   超むずかしい

内部では10 Tierで細かく調整します。

### 難易度テーブル

  ---------------------------------------------------------------------------------------------------
  LEVEL     表示                   T字目標   立体交差目標   一方通行目標      固定数 主な変化
  --------- ------------------ ----------- -------------- -------------- ----------- ----------------
  1--10     やさしい                     0              0              0           0 基本配線

  11--20    やさしい                  0--1              0              0        0--1 T字導入候補

  21--30    ふつう                    1--2           0--1              0        0--1 立体交差導入

  31--40    ふつう                    1--2           0--2           0--1        0--1 一方通行導入

  41--50    むずかしい                1--2           0--2              1        0--2 複合化開始

  51--60    むずかしい                1--2           0--2           1--2        0--2 一方通行増加

  61--70    とてもむずかしい          1--3           1--2           1--2        1--2 立体交差を常用

  71--80    とてもむずかしい          1--3           1--3           1--2        1--2 複合構造増加

  81--90    超むずかしい              1--3           1--3           1--3        1--3 高密度化

  91--100   超むずかしい              2--3           1--4           2--3        1--3 最終複合帯
  ---------------------------------------------------------------------------------------------------

**注意:**
これらは「必ずその個数を入れる」という意味ではなく、生成時の目標範囲です。品質条件を満たせない場合は無理に特殊ギミックを詰め込まない方針です。

------------------------------------------------------------------------

## 17. 難易度の内部パラメータ

特殊ギミック数以外にも以下を使っています。

### `complexityRange`

T字・立体交差・一方通行を組み合わせた構造負荷の目安。

構成スコアは現在、

``` text
T字 × 2
+ 立体交差
+ 一方通行
```

を基本に評価します。

T字を重く評価しているのは、同番号ライトを複数成立させる必要があり、判断負荷が大きいためです。

### `minTurns`

正解ルートに求める曲がり数の目安。

高LEVELほど増えます。

### `maxStraightRun`

長い直線ばかりになることを抑える指標。

後半ほど直線依存を減らします。

### `minBacktracks`

上方向への折り返しなど、単純に下へ進むだけではない経路を増やすための指標。

### `fixedTotalRange`

固定ブロック数。

固定が多すぎるとルート推測が簡単になるため、最大でも少量にしています。

### `minBranches` / `minFakeDepth`

ダミー構造の複雑さに関する内部目標値です。現在のクリーンジェネレータでは、過去ロジック由来の難易度指標として残っている部分もあります。

------------------------------------------------------------------------

## 18. ステージ構成タイプ

`chooseStructurePlan()` では難易度に応じて構成タイプを選びます。

主なタイプ:

-   `basic`
-   `branch`
-   `bridge`
-   `diode`
-   `balanced`

難易度が上がるほど複数タイプが候補になります。

例:

-   `branch`: T字をやや重視
-   `bridge`: 立体交差をやや重視
-   `diode`: 一方通行をやや重視
-   `balanced`: バランス型

これにより、同じ難易度帯でもステージの特徴が完全に同じにならないようにしています。

------------------------------------------------------------------------

## 19. 図鑑・初登場ギミック

図鑑表示は固定LEVEL判定ではありません。

ステージ生成後に実際の盤面を走査し、

-   電源
-   ゴール
-   ケーブル
-   ライト
-   固定
-   T字
-   一方通行
-   立体交差

のうち、**その盤面に実在し、まだ図鑑表示していないもの**だけを表示します。

したがって、生成結果によって特殊ギミックの初登場LEVELが多少変化しても対応できます。

------------------------------------------------------------------------

## 20. 100ステージと周回

### 初回

1周目は通常の100ステージとして進行します。

### 100ステージクリア後

次の周回へ進むと `generationCycle` が増加します。

次周回ではシードが変わるため、100ステージを新しい盤面で再プレイできます。

### 周回表示

1周目は周回表示を目立たせず、2周目以降に「2周目」「3周目」などが分かる設計です。

------------------------------------------------------------------------

## 21. 保存データ

主な保存項目はブラウザのストレージに保持します。

代表例:

-   解放済みLEVEL
-   周回数 `generationCycle`
-   周回ごとのクリア済みステージ
-   図鑑既読状態
-   BGM ON/OFF
-   SE ON/OFF
-   BGM音量
-   SE音量

クリア済みステージは周回単位でキーを分けています。

概念:

``` text
electricLinkClearedStagesV1_cycle_0
electricLinkClearedStagesV1_cycle_1
electricLinkClearedStagesV1_cycle_2
...
```

### ローカルHTML

`file://` で直接HTMLを開いた場合、一部ブラウザでは `localStorage`
が制限される可能性があります。

その場合は `safeStorage`
がページ内メモリへフォールバックし、ゲーム自体が停止しないようにしています。

ただしメモリフォールバック時は、ブラウザを閉じた後の永続保存は保証されません。

------------------------------------------------------------------------

## 22. デバッグモード

HTML内の以下のフラグで切り替えます。

``` js
const DEBUG_MODE = false;
```

`true` にするとデバッグ用機能を利用する想定です。

主な用途:

-   全ステージ確認
-   周回・進行状態確認
-   保存状態リセット
-   正解ルート表示
-   ステージ生成確認

正解ルート表示では、通常配線と区別できる黄色系のオーバーレイで正解ネットワークを表示します。

------------------------------------------------------------------------

## 23. タイトル画面・オーディオ

### タイトル

ゲーム開始時にタイトル画面を表示します。

保存データがある場合は、

``` text
CONTINUE · LEVEL xx · n周目
```

のように進行状況を表示します。

### GAME START音

開始時は専用の起動SEを再生し、その後BGMを開始します。

### 音量

現在の基準値:

``` text
MASTER_GAIN = 1.75
BGM_SOURCE_GAIN = 2.0
```

急なピークを抑えるためDynamics Compressorを通しています。

------------------------------------------------------------------------

## 24. 初期ギミック説明

新しいギミックが初めて実際の盤面に登場した場合、ステージ開始時に図鑑形式の説明を表示します。

固定LEVELに依存しないため、

> LEVEL 30だから立体交差説明を出す

ではなく、

> 今回生成された盤面に立体交差が実際に存在するので、未読なら説明を出す

という方式です。

------------------------------------------------------------------------

## 25. 生成品質の基本原則

今後ステージ生成を変更する際は、以下を不変条件として扱うことを推奨します。

### 必須条件

1.  0ポートの通常ブロックを生成しない。
2.  1ポート通常ケーブルを生成しない。
3.  必須ライトは2ポート。
4.  ライト番号は1から連番。
5.  本線上のライト番号は進行方向に増加する。
6.  同番号ライトはすべて成立してから次番号へ進む。
7.  T字の両側で異なる番号を直接要求しない。
8.  T字ダミー末端を不自然な片側ケーブルにしない。
9.  立体交差は盤面端に配置しない。
10. 立体交差は両軸を利用できる構造だけ採用する。
11. 一方通行は実際の進行方向と矛盾しない。
12. STARTから必須ライトへ物理的に到達できる。
13. STARTからGOALへ物理的に到達できる。
14. 最初からクリア済み状態にしない。
15. 特殊ギミック数より盤面品質を優先する。
16. 再生成を無限ループさせない。
17. 同一周回・同一LEVELは決定論的に再現できる。

------------------------------------------------------------------------

## 26. 今後変更する際の推奨方針

### 「生成」と「検証」を分離する

推奨:

``` text
generate()
  ↓
validate()
  ↓
OK → adopt()
NG → 別候補
```

避けたい方式:

``` text
generate()
  ↓
問題発見
  ↓
その場で部品を書き換える
  ↓
別の整合性が壊れる
  ↓
さらに補正
```

### 番号は後から変更しない

ライト番号は正解経路の構造そのものなので、生成後の番号補正はT字や通電ゲートとの不整合を起こしやすくなります。

### 特殊ギミックは「個数」より「成立」を優先する

例えば、

``` text
T字を必ず3個
```

ではなく、

``` text
T字目標1～3個
ただし成立条件を満たすものだけ採用
```

とします。

### 監査ログを残す

不具合報告時に、

``` js
window.__lastGenerationAudit
```

を確認できるようにしておくと、LEVEL・シード・ライト番号・ギミック数・経路長を追いやすくなります。

------------------------------------------------------------------------

## 27. 現在の設計上の注意点

Ver.1.4.85は生成コアを整理した段階であり、今後さらにテストを重ねる場合は、特に以下を重点確認すると安全です。

-   100ステージ × 複数周回の自動生成監査
-   T字が複数存在するステージ
-   T字と立体交差が同時に存在するステージ
-   T字と一方通行が同時に存在するステージ
-   立体交差が複数存在する後半ステージ
-   同番号ライトが複数ある状態で前段を切断した場合
-   ローカルHTML起動時の保存挙動
-   スマートフォンでの盤面リサイズ
-   100ステージクリア→次周回移行

------------------------------------------------------------------------

## 28. バージョン管理時の考え方

GitHub等で公開する場合、HTML本体に加えて本仕様書を保存しておくと、生成ロジック変更時の意図を追いやすくなります。

推奨構成:

``` text
/
├─ index.html
├─ README.md
├─ GAME_SPEC.md
└─ LICENSE または権利表記資料
```

本資料は `GAME_SPEC.md` として利用できます。

生成ロジックを変更した場合は、HTMLのバージョンと本資料の対象バージョンを合わせて更新することを推奨します。

------------------------------------------------------------------------

## 29. 仕様要約

Electric Linkの現在のステージ生成で最も重要な考え方は、

> **「特殊ギミックをたくさん置くこと」ではなく、「論理的に成立した回路を作り、そのうえで迷わせること」**

です。

難易度は単純なブロック数ではなく、

-   盤面サイズ
-   正解経路の曲がり
-   ダミー配線
-   T字による同番号複数条件
-   立体交差
-   一方通行
-   少量の固定配線
-   複数ギミックの組み合わせ

によって段階的に上げます。

特にT字・ライト番号・立体交差は回路ルールそのものに影響するため、後処理で形を整えるのではなく、**正しいトポロジーを生成したうえで採用する**ことを基本方針とします。
