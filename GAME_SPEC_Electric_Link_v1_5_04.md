# Electric Link 仕様・生成ロジック資料

> 対象バージョン: **Ver.1.5.04**  
> 対象ファイル: `electric_link_v1_5_04_revisit_to_t_fallback.html`  
> 本資料は、現行HTML実装を基準に、ゲーム機能・通電ルール・ステージ生成・難易度・ギミック・保存・デバッグ・品質監査を整理したものです。  
> 旧 `GAME_SPEC_Electric_Link_v1_4_86.md` をベースに、Ver.1.5.04までの仕様変更を反映しています。

---

## 1. ゲーム概要

**Electric Link** は、盤面上のケーブルブロックを90°ずつ回転させ、電源から番号ライトを順番に通電し、最後にゴールへ電気を届ける回路パズルです。

基本目標:

1. 電源から正しい回路を構築する。
2. 番号ライトを **1 → 2 → 3 → ...** の順番で成立させる。
3. 同じ番号のライトが複数ある場合は、その番号のライトをすべて成立させる。
4. すべての必須ライトが点灯した状態でゴールへ通電する。
5. ステージをクリアして次のステージを解放する。

全100ステージ構成で、LEVEL 100クリア後は生成周回を進め、別盤面の100ステージを再プレイできます。

---

## 2. 基本操作

### 2.1 ケーブル操作

- 回転可能なブロックをタップすると90°回転する。
- 固定ブロックは回転できない。
- 電源・ゴールなど、ゲーム上回転させない部品がある。
- 同一周回・同一LEVELでは、採用済み生成シードと初期スクランブルが決定論的に再現される。
- 「このレベルをやり直す」は、そのLEVELの開始時配置へ戻す。

### 2.2 ステージ選択

- 全100LEVEL。
- 10LEVEL単位でページ表示。
- `◀ / ▶` で前後10LEVELへ移動。
- 通常モードでは未解放LEVELは選択不可。
- クリア済みLEVELには専用表示を付ける。
- デバッグモードでは全100LEVELを選択可能。

---

## 3. 通電・番号順・クリア条件

### 3.1 物理接続

電源から探索し、隣接ブロックについて以下を満たした場合に通電します。

- 現在ブロックが隣方向へのポートを持つ。
- 隣ブロックが反対方向へのポートを持つ。
- 一方通行の方向条件を満たす。
- 立体交差では、入ってきた軸から別軸へ曲がらない。

### 3.2 ライトを「通過した電流」の番号状態

Ver.1.4.86以降の番号制御では、単純に「前の番号が画面上で点灯済みだから後番号を許可する」のではなく、**その電流がどの番号まで正しく通過したか**を状態として扱います。

原則:

```text
passed = 0
  ↓
①を実際に通る
  ↓
passed = 1
  ↓
②を実際に通る
  ↓
passed = 2
  ↓
③ ...
```

禁止事項:

- ②を通った電流が③を通らず④へ入る。
- ③まで進んだ電流が②側へ戻り、その②側から④へ抜ける。
- 先の番号から前の番号へ逆流して順序状態を再利用する。

次に入れる必須ライトは、基本的に **`passed + 1` の番号だけ** です。

### 3.3 同番号ライト

T字などにより同番号ライトが複数ある場合は、その番号グループを1セットとして扱います。

```text
        ┌─ ②
① ─ T ─┤
        └─ ②

②を両方成立
      ↓
③へ進行
```

片側だけでは次番号を完成扱いにしません。

### 3.4 ライト充電

必須ライトは、接続した瞬間ではなく一定時間通電を維持すると点灯します。

現行の基準値:

```text
chargeMs = 520
```

前段の接続が崩れた場合は、後続番号を再ロックし、必要に応じて充電状態を戻します。

### 3.5 クリア条件

- 必須ライトがすべて成立している。
- ゴールへ有効な番号順電流が到達している。
- 立体交差を含む回路では、軸分離ルールを含めた物理接続監査を通過した盤面である。

---

## 4. ブロック・ギミック仕様

### 4.1 電源

- 回路開始地点。
- 基本1ポート。
- 回転不可。
- 通電探索の起点。

### 4.2 ゴール

- 回路最終地点。
- 基本1ポート。
- 必須ライト成立後の有効電流を到達させる。

### 4.3 通常ケーブル

通常形状:

- 縦ストレート
- 横ストレート
- L字4方向

通常ケーブルは原則 **2ポート**。

正常生成では、0ポートの `・` や、意味のない1ポート通常ケーブルを採用しません。

### 4.4 番号ライト

- 必須ライトは2ポート。
- 本線進行順に番号を割り当てる。
- 番号は生成時に確定し、後処理で振り直さない。
- `1 → 3` のような欠番を正常採用しない。
- 同番号ライトが複数ある場合は全灯が必要。

---

## 5. T字分岐

### 5.1 基本ルール

T字は3ポートの正解ネットワーク部品で、単純な分岐ではなく **同番号ライトのペア構造**として生成します。

```text
                   ┌─ ② ─ ダミー続行
① ─── T字 ────────┤
                   └─ ② ─ 正規ルート → ③
```

必須条件:

- 分岐先のライト番号は同じ。
- `T字 → ② / ③` のような異番号直接分岐は禁止。
- 同番号の両ライト成立後に次番号へ進む。
- T字自体を固定ブロックとして扱わない。
- 枝が盤面外・本線・他枝・特殊部品と不正衝突する場合は採用しない。

### 5.2 長いダミールート

Ver.1.5.03以降、T字ダミー側を「ライトの次の1ブロックで即終了」させません。

**ダミーライト通過後の目標長:**

| LEVEL | ダミーライト後の長さ |
|---|---:|
| 11–30 | 2–3ブロック |
| 31–60 | 3–4ブロック |
| 61–80 | 3–5ブロック |
| 81–100 | 4–6ブロック |

設計意図:

- プレイヤーがT字を見ただけで即ダミー側を判別できない。
- ダミーライトまでは正しい通電対象。
- その先を数ブロック追ってから「この先は正解へつながらない」と判断させる。
- ダミー枝途中では直進だけでなく曲がりも許可。
- 盤面端で露骨に終了する枝は避ける。
- 正解本線へ不正に再接続しない。
- 末端は1ポートにせず、さらに続きそうに見える2ポート形状を基本とする。

### 5.3 T字ダミー長監査

生成後に `dummyLength` を監査し、難易度帯の最低値より短いT字ダミーは盤面NGとします。

---

## 6. 立体交差

立体交差は縦軸と横軸が中央で電気的に接続せず、別々に直進する4ポート部品です。

### 6.1 共通ルール

- 4ポート。
- 盤面端には置かない。
- 通電時は縦・横の軸を分離。
- 横から入った電流は横へ、縦から入った電流は縦へ直進。
- 中央で90°ターンしない。
- 上下左右の接続が物理的に成立する構造のみ採用する。
- 正解path上で直交2回通過が検出されたセルは必ず `bridge` へ昇格させる。

### 6.2 3種類の利用方法

#### A. 正規ルート利用型

正解本線が一方の軸を通り、もう一方の軸は独立した配線として存在します。

#### B. ダミー利用型

正解本線とは関係しない場所に、縦横両軸を持つダミー交差を配置します。

#### C. 再訪交差

同じ座標を、正解ルートが異なるタイミングで別軸から2回通過します。

例:

```text
1回目: 左 → [交差] → 右
                ↓
        盤面を別経路で進む
                ↓
2回目: 上 → [同じ交差] → 下
```

通常セルは原則1回だけ使用し、**立体交差だけを「横軸1回 + 縦軸1回」の再訪可能セル**として扱います。

### 6.3 再訪交差の生成思想

再訪には以下の2系統があります。

- 既存正解pathの形から「過去ルートを横から通過できる場所」を見つけて再訪化。
- 再訪を前提に `START → 交差1回目 → 迂回 → 交差2回目 → GOAL` を経路探索で構築。

固定の横ジグザグテンプレートにはせず、START/GOAL・交差位置・迂回方向・中継点をシードで変化させます。

後半では、過去のルートを横から横切るような再訪も重要な生成パターンです。

### 6.4 再訪交差監査

再訪セルについて以下を確認します。

- 正解path上で同一セルが2回登場。
- 2回の通過軸が直交。
- 盤面上の部品が `bridge`。
- `revisitBridge` 属性が有効。
- 4ポートを持つ。
- 上下左右4方向が実際に隣接配線と接続。
- 同一セルが3回以上登場しない。
- 同軸で2回通過する不正再訪ではない。

監査NGの再訪交差を通常wireへ降格して採用することは禁止しています。

---

## 7. 一方通行

- 2ポート。
- 指定方向にのみ通電可能。
- 現在のクリーン生成では、主に正解本線上のストレート2ポートセルを候補にする。
- 進行方向から `input` / `forward` を決定。
- 逆方向からは通電しない。
- ライト・T字・立体交差など他の特殊部品と重複させない。

---

## 8. 固定ブロック

- 回転不可。
- 🔒表示。
- 個数達成より配置品質を優先。
- 盤面端の明らかに意味のない場所には置かない。
- START/GOAL・必須ライト直近など、露骨に正解ヒントになる場所を避ける。
- 正解側固定は増やしすぎない。
- ダミー側固定も正解ルートから大きく離れた無意味な場所には置かない。
- 候補不足なら設定上限まで無理に配置しない。

---

## 9. ステージ生成の基本フロー

Ver.1.5.04の概念フロー:

```text
LEVEL / 周回
    ↓
基礎シード・採用済みシード確認
    ↓
盤面サイズ
    ↓
難易度ルール
    ↓
構成タイプ選択
    ↓
正解path候補生成
    ↓
経路品質チェック
    ↓
再訪交差候補 / 再訪前提ルート
    ↓
ライト番号配置
    ↓
T字生成
    ↓
通常正規立体交差 / ダミー立体交差
    ↓
一方通行
    ↓
正解ネットワーク確定
    ↓
残りセルを2ポートダミーで充填
    ↓
固定ブロック
    ↓
完成盤面監査
    ↓
NGなら別候補・別シード
    ↓
採用シード固定
    ↓
初期配置スクランブル品質探索
    ↓
プレイ開始
```

重要方針:

> **不正な特殊部品を後処理で通常部品へ書き換えて採用するのではなく、監査NGなら盤面自体を不採用にする。**

---

## 10. シード・周回・再現性

### 10.1 基礎シード

`STAGE_REBUILD_EPOCH` とLEVEL番号から100ステージ分の `STAGE_LAYOUT_SALTS` を決定論的に生成します。

### 10.2 周回シード

概念的には:

```text
基礎ステージシード
+ generationCycle × 1,000,003
```

周回が変わると同LEVELでも別盤面になります。

### 10.3 採用済みシード固定

難しい生成条件では複数候補を試すため、単純に基礎シードだけでは「選び直したとき別盤面になる」問題が発生し得ます。

Ver.1.5系では、監査を通過した `generationSalt` を、

```text
周回 + LEVEL
```

単位の保存キーへ記録します。

Ver.1.5.04系のキー概念:

```text
electricLinkAcceptedSalt_v154_..._cycle_<cycle>_level_<level>
```

これにより、同じ周回・同じLEVELを再選択しても、採用済み盤面を再現します。

---

## 11. 正解ルート生成

### 11.1 基本候補

`makeSerpentinePath()` をベースに、複数のルートファミリー・向きを生成します。

### 11.2 START / GOAL方向の多様化

単純な上→下固定を避けるため、以下を組み合わせます。

- 縦型生成
- 横型生成（座標転置）
- 左右反転
- 上下反転
- START/GOAL反転
- 複数経路ファミリー

これにより、上→下・下→上・左→右・右→左などが混在します。

### 11.3 クリーン経路検査

通常path候補では以下を確認します。

- 配列として成立。
- 必要長を満たす。
- 盤面内。
- 通常pathではセル重複なし。
- 連続セルが上下左右で隣接。

最低経路長の目安:

```text
max(14, ライト数 × 5 + 5)
```

### 11.4 安全経路

通常候補が成立しない場合に `safeWeavePath()` を使用する経路がありますが、後半でこれだけに依存して単純化しないよう、特殊ギミックの難易度下限監査を別途行います。

---

## 12. ライト生成

- 本線順に番号配置。
- 各ライト間に最低限の距離を確保。
- 特殊部品セルをライトで上書きしない。
- 立体交差中心をライト化しない。
- 番号は生成後に振り直さない。
- ユニーク番号列が `1,2,3,...` か監査する。

---

## 13. ライト順トポロジー監査

完成盤面に対して、各番号が前番号から正しく到達可能かを検証します。

確認例:

- ①から②へ到達できる。
- ②から③へ到達できる。
- ③を通らず④へ行くショートカットがない。
- 同番号ライトはすべて前番号から到達可能。
- 最終番号群からGOALへ到達可能。

`② → ④` のような番号ショートカットが物理ネットワーク上に存在する場合は監査NGです。

---

## 14. ダミー配線

正解ネットワーク以外の空きセルは、2ポートの通常ケーブルで埋めます。

使用形状:

```text
縦ストレート
横ストレート
L字 × 4方向
```

原則:

- 0ポートを生成しない。
- 一般ダミーに1ポートを使わない。
- 正解に見える接続候補を作る。
- T字ダミー、立体交差ダミーなど、役割付きダミーは属性を持たせてデバッグ表示可能にする。

---

## 15. 初期配置スクランブル

Ver.1.4.89以降は「完成していなければよい」ではなく、**開始時の接続そのものを少なくする**方針です。

### 15.1 品質指標

初期状態について概念的に、

- 全隣接接続数
- 正解ネットワーク同士の接続数
- ライトなど重要部品周辺の接続数
- 電源から初期状態で到達してしまうセル数

を評価します。

特に電源から最初から先へ流れる状態を重くペナルティ化します。

### 15.2 複数候補

- 複数の決定論的スクランブル候補を生成。
- 各ブロックの回転方向を比較し、接続スコアを下げる。
- 最も開始時接続が少ない候補を採用。
- 同LEVEL・同周回では結果を再現可能。

狙いは「全セルを完全孤立させる」ことではなく、**正解ルートが最初から連続して見える状態を極力なくすこと**です。

---

## 16. 盤面サイズ

| LEVEL | 列 × 行 |
|---|---|
| 1–10 | 6 × 8 |
| 11–20 | 7 × 8 |
| 21–30 | 7 × 9 |
| 31–40 | 7 × 10 |
| 41–50 | 7 × 11 |
| 51–60 | 8 × 11 |
| 61–70 | 8 × 12 |
| 71–80 | 8 × 12 |
| 81–90 | 8 × 12 |
| 91–100 | 8 × 12 |

LEVELが上がる途中で盤面が小さく戻らない構成です。

---

## 17. 難易度設計

100ステージを10帯に分け、プレイヤー表示は5段階です。

- やさしい
- ふつう
- むずかしい
- とてもむずかしい
- 超むずかしい

内部は10 Tierです。

### 17.1 難易度テーブル

| LEVEL | 表示 | T字目標 | 立体交差目標 | 一方通行目標 | 固定目標 | 主な変化 |
|---|---|---:|---:|---:|---:|---|
| 1–10 | やさしい | 0 | 0 | 0 | 0 | 基本配線 |
| 11–20 | やさしい | 0–1 | 0 | 0 | 0–1 | T字導入候補 |
| 21–30 | ふつう | 1–2 | 0–1 | 0 | 0–1 | 立体交差導入 |
| 31–40 | ふつう | 1–2 | 0–2 | 0–1 | 0–1 | 再訪低確率・一方通行導入 |
| 41–50 | むずかしい | 1–2 | 0–2 | 1 | 0–2 | 再訪比率上昇 |
| 51–60 | むずかしい | 1–2 | 0–2 | 1–2 | 0–2 | 再訪・一方通行増加 |
| 61–70 | とてもむずかしい | 1–3 | 1–2 | 1–2 | 1–2 | 立体交差常用 |
| 71–80 | とてもむずかしい | 1–3 | 1–3 | 1–2 | 1–2 | 再訪・複合構造 |
| 81–90 | 超むずかしい | 1–3 | 1–3 | 1–3 | 1–3 | 高密度複合 |
| 91–100 | 超むずかしい | 2–3 | 1–4 | 2–3 | 1–3 | 最終複合帯 |

これらは生成「目標」であり、個数を無理に満たすために不正構造を作ることはしません。

---

## 18. 立体交差の難易度進行

### LEVEL 1–21

- 原則立体交差なし。

### LEVEL 22

- 立体交差の初登場。
- **再訪交差を確定させる特例は廃止済み。**
- 初回は通常の正規ルート利用型を優先。
- 「初登場なのに完全ダミーだけ」で理解しづらくならないようにする。

### LEVEL 23–30

- 正規利用型 / ダミー型を混在。
- 立体交差が1個だけの場合、前半帯では正規利用型を比較的優先。
- 再訪はまだ強制しない。

### LEVEL 31–40

再訪候補は約25%。

### LEVEL 41–50

再訪候補は約50%。

### LEVEL 51–60

再訪候補は約2/3。

### LEVEL 61–80

再訪を主要候補として扱う。立体交差主体ステージでは複数再訪を試す場合がある。

### LEVEL 81–100

再訪を主要な難易度要素として扱う。

91–100では立体交差自体が0個の盤面は監査NGです。

---

## 19. T字と再訪交差のバランス

再訪交差とT字を無制限に同時投入すると情報過多になるため、通常は再訪側を優先します。

一方、Ver.1.5.04では重要なフォールバックを追加しています。

### 再訪成功

```text
再訪交差を主役
  ↓
T字数を抑える
```

### 再訪失敗

```text
再訪交差を狙った
  ↓
成立しない
  ↓
難易度負荷をT字へ振り替える
```

再訪を狙ったにもかかわらず0個だった場合:

- T字を最低1個狙う。
- 条件があれば元のT字計画 + 1個を狙う。
- T字の成立上限はライト数・候補位置による。
- T字フォールバック時は長いダミー枝仕様をそのまま使用。

---

## 20. 難易度の底抜け防止

Ver.1.5.04の重要仕様です。

### LEVEL 31以降

原則として、

```text
T字 = 0
かつ
立体交差 = 0
```

の盤面は採用しません。

### 再訪失敗時

```text
再訪交差不成立
かつ
T字振替も0
```

も監査NGです。

### LEVEL 61–90

旧仕様の、

> 特殊ギミック生成に失敗したら特殊要素なしの安全盤面へ落とす

方式を廃止。

現在は、

> 再訪/立体交差側が難しければ **T字主体のフォールバック生成**

を行います。

これにより、後半で突然「T字も立体交差もない単純ステージ」になることを避けます。

### LEVEL 91–100

有効な立体交差を最低1個要求します。

---

## 21. 構成タイプ

`chooseStructurePlan()` では主に以下を使います。

- `basic`
- `branch`
- `bridge`
- `diode`
- `balanced`

傾向:

- `branch`: T字寄り
- `bridge`: 立体交差寄り
- `diode`: 一方通行寄り
- `balanced`: 複合型

内部の構成負荷目安:

```text
T字 × 2
+ 立体交差
+ 一方通行
```

T字は同番号ライトを複数成立させる必要があるため、比較的重い負荷として評価しています。

---

## 22. 難易度内部パラメータ

### `complexityRange`

特殊ギミックの総合負荷。

### `minTurns`

正解ルートに求める曲がり数の目安。

### `maxStraightRun`

長い直線が続きすぎないようにする目安。

### `minBacktracks`

単純に一方向へ進むだけではない折り返し量の目安。

### `fixedTotalRange`

固定ブロックの目標範囲。

### `minBranches` / `minFakeDepth`

ダミー構造の複雑さに関する難易度指標。現行クリーンジェネレータでは一部旧ロジック由来の指標も残ります。

---

## 23. 完成盤面の品質監査

生成後・初期スクランブル前に監査します。

### 23.1 ポート数

| 部品 | 原則ポート数 |
|---|---:|
| 通常ケーブル | 2 |
| T字 | 3 |
| ライト | 2 |
| 一方通行 | 2 |
| 立体交差 | 4 |
| 電源 | 1 |
| ゴール | 1 |

### 23.2 主な監査

- ライト番号が連番。
- T字枝ライトと本線ライトが同番号。
- T字ダミーが規定長以上。
- 番号ショートカットがない。
- 必須ライトへ順番通り到達可能。
- 最終ライトからGOALへ到達可能。
- 正解path重複がある場合、正当な再訪立体交差か。
- 再訪セルがbridgeへ昇格しているか。
- 再訪bridgeの4方向接続が成立しているか。
- LEVEL 31以降でT字0・立体交差0になっていないか。
- 再訪不成立時にT字振替が成立しているか。
- LEVEL 91–100で有効立体交差が最低1個あるか。

監査結果は:

```js
window.__lastGenerationAudit
```

へ保持します。

代表情報:

- LEVEL
- 採用シード
- issues
- T字数
- T字ダミー長
- 立体交差数
- 正規立体交差数
- ダミー立体交差数
- 再訪交差数
- 再訪交差位置・再訪間隔
- 一方通行数
- 固定数
- ライト番号列
- 正解path長
- pathシード
- path方向
- Tフォールバック状態
- 再訪失敗状態

---

## 24. 生成失敗時の再試行

基本思想:

```text
生成
 ↓
監査
 ├─ OK → 採用
 └─ NG → 別シードで再生成
```

禁止方針:

```text
NGの立体交差を通常wireへ降格
 ↓
正解pathだけ再訪のまま残す
 ↓
クリア不能
```

このような「特殊部品だけ書き換えて見かけ上復旧」する方式は使用しません。

特に再訪交差では、成立しなければ盤面を再生成するか、難易度負荷をT字へ振り替えます。

---

## 25. 初登場ギミック・図鑑

固定LEVEL表ではなく、**生成後の実盤面を走査**して初登場判定します。

対象:

- 電源
- ゴール
- ケーブル
- 番号ライト
- 固定
- T字
- 一方通行
- 立体交差

その盤面に実際に存在し、まだ未読の要素だけ図鑑表示します。

### 立体交差初登場

LEVEL 22を立体交差の初登場として生成側で優先していますが、説明文は「再訪専用」ではありません。

説明内容:

- 縦横は中央で接続しない。
- 正規利用型とダミー型がある。
- 難易度上昇後には再訪型も登場する。

---

## 26. 100ステージと周回

### 1周目

通常の100ステージ。

### LEVEL 100クリア後

- `generationCycle` を増やす。
- 新しいシード群で100LEVELを再プレイ。
- 1周目は周回表示を控えめにする。
- 2周目以降は「2周目」「3周目」等を表示。

---

## 27. 保存データ

主な保存項目:

- 解放済みLEVEL
- `generationCycle`
- 周回別クリア済みLEVEL
- 図鑑既読状態
- BGM ON/OFF
- SE ON/OFF
- BGM音量
- SE音量
- 採用済みステージ生成シード

クリア履歴:

```text
electricLinkClearedStagesV1_cycle_0
electricLinkClearedStagesV1_cycle_1
...
```

採用シードも周回・LEVEL単位に保存するため、同ステージを選び直して構成が変わることを防ぎます。

### `file://` 直開き

`localStorage` が使える場合は永続保存。

使用不可の場合は `safeStorage` がページ内メモリへフォールバックし、ゲーム停止を防ぎます。

メモリフォールバック時はブラウザを閉じた後の永続保存は保証されません。

---

## 28. デバッグモード

HTML内:

```js
const DEBUG_MODE = false;
```

公開版は原則 `false`。

`true` の場合:

- 全100LEVEL解放。
- 周回数変更。
- 1周目へ戻す。
- 現周回クリア履歴削除。
- 全100LEVELクリア済みにする。
- 図鑑を全既読にする。
- 図鑑を未読へ戻す。
- 全セーブ初期化。
- ステージ再作成。
- 正解ルート表示。
- ダミールート表示。

### 28.1 正解ルート表示

- 黄色系オーバーレイ。
- ON/OFFトグル。
- 一度ONにするとステージ変更後もON状態を維持。
- 新ステージではそのステージの正解ネットワークを表示。
- 再度押すとOFF。

### 28.2 ダミールート表示

- マゼンタ系オーバーレイ。
- 正解ルートとは独立したON/OFF。
- 正解とダミーを同時表示可能。
- ステージ変更後もON状態を維持。
- 正解ネットワークと重なる辺は正解側表示を優先。
- T字の長いダミー枝も表示対象。
- ダミー立体交差の配線も確認可能。

---

## 29. タイトル画面

ゲーム起動時にタイトル画面を表示します。

保存進行がある場合:

```text
CONTINUE · LEVEL xx · n周目
```

等を表示。

現在タイトルには著作権表示を含みます。

---

## 30. オーディオ

### GAME START

開始ボタンでは専用起動SE:

```text
カチッ
 ↓
電力チャージ音が上昇
 ↓
バシュン
 ↓
BGM開始
```

BGM開始は起動インパクト後に入る設計です。

### 基準ゲイン

```text
MASTER_GAIN = 1.75
BGM_SOURCE_GAIN = 2.0
```

Dynamics Compressorを通し、大きなピークを抑えます。

保存対象:

- BGM ON/OFF
- SE ON/OFF
- BGM volume
- SE volume

---

## 31. プレイヤー向け目標・ポイント表示

右側の目標表示では、内部生成パラメータをそのまま見せない方針です。

避ける例:

```text
ULTIMATE：T字1〜3・立体交差0〜4...
```

プレイヤー向けには、

- 難易度
- そのステージで意識すべき考え方
- 実際に採用されたギミックに対応するポイント

を表示します。

存在しない立体交差について「立体交差を見落とさない」と出すなど、盤面実態と矛盾するメッセージは避けます。

再訪交差が主要要素の場合は「一度通った立体交差を別軸から再利用する可能性」などを示します。

---

## 32. 生成品質の不変条件

今後の変更でも維持するべき条件:

1. 0ポート通常ブロックを生成しない。
2. 意味のない1ポート通常ケーブルを生成しない。
3. 必須ライトは2ポート。
4. ライト番号は1から連番。
5. 番号順を実電流状態で守る。
6. 番号飛ばしを許可しない。
7. 前番号への逆流で後番号を開けない。
8. 同番号ライトは全灯後に次段階へ進む。
9. T字の両枝は同番号ライト。
10. T字ダミーを短すぎる即死枝にしない。
11. T字ダミー末端を1ポートにしない。
12. 立体交差は盤面端に置かない。
13. 立体交差の縦横を中央で接続しない。
14. 再訪セルは必ず立体交差へ昇格する。
15. 不正再訪を通常wireへ降格して採用しない。
16. 一方通行は本線進行と矛盾しない。
17. 固定ブロックを無意味な盤面端へ置かない。
18. STARTから必須ライトへ到達可能。
19. 最終番号からGOALへ到達可能。
20. 初期状態で正解ルートが大きくつながった状態を避ける。
21. 同一周回・同一LEVELの盤面を安定再現する。
22. LEVEL 31以降でT字0・立体交差0の難易度底抜けを避ける。
23. 再訪失敗時はT字へ負荷を振り替える。
24. LEVEL 91–100は有効な立体交差を最低1個持つ。
25. 生成監査NGを無理な後処理で隠さない。

---

## 33. 生成ロジック変更時の推奨原則

### 生成と検証を分離

推奨:

```text
generate()
   ↓
validate()
   ↓
OK → adopt()
NG → retry / alternate structure
```

避ける:

```text
generate()
   ↓
問題発見
   ↓
その場で部品種類だけ変更
   ↓
正解pathとの整合性が壊れる
```

### 「目標個数」と「最低難易度」を分ける

特殊ギミック数は目標値ですが、後半で負荷が完全に消えないよう、別途「難易度の床」を監査します。

例:

```text
再訪目標1
  ↓
再訪不成立
  ↓
T字1以上へ振替
```

これにより「品質優先」と「後半なのに簡単すぎない」を両立します。

---

## 34. 現在の重点テスト項目

Ver.1.5.04で特に確認したい項目:

- 100LEVEL × 複数周回の決定論的再現。
- LEVEL 31以降にT字0・立体交差0が混入しないか。
- 再訪失敗後にT字フォールバックが実際に成立しているか。
- T字ダミー長が難易度帯の最低長を満たすか。
- T字ダミーが本線へ意図せず再接続しないか。
- 同番号ライトの両方が成立しないと次番号へ進めないか。
- ②から③を通らず④へ行くショートカットがないか。
- 再訪セルが通常wireのまま残らないか。
- 再訪bridgeの4方向接続が成立するか。
- 立体交差の縦横が通電表示上でも混線しないか。
- LEVEL 22の初立体交差が再訪固定になっていないか。
- 23–60でダミー立体交差だけが長期間連続しないか。
- 91–100で立体交差0にならないか。
- 同じLEVELを選び直して構成が変化しないか。
- 正解ルート表示 / ダミールート表示がステージを跨いで維持されるか。
- 初期状態で正解ルートが最初から連続接続していないか。
- スマートフォンで盤面フィット・拡大表示が崩れないか。
- `file://` 直開き時にストレージ制限で停止しないか。
- LEVEL 100クリアから次周回への移行。

---

## 35. GitHub公開時の構成

推奨:

```text
/
├─ index.html
├─ README.md
├─ GAME_SPEC.md
└─ LICENSE または権利表記資料
```

本ファイルは `GAME_SPEC.md` として利用できます。

HTMLの生成バージョンを更新した場合は、本仕様書の対象バージョンも合わせて更新してください。

---

## 36. Ver.1.4.86 → Ver.1.5.04 主な更新点

- ライト順判定を「点灯済み状態」だけでなく、実際に番号を通過した電流状態へ強化。
- 番号逆流・番号ショートカット監査を追加。
- 立体交差の軸分離通電を強化。
- 正規・ダミー・再訪の3種類の立体交差運用へ拡張。
- 再訪交差を正解path実体から再検出し、必ずbridgeへ昇格。
- 再訪生成専用ルートの固定ジグザグ化を廃止し、探索型へ変更。
- START/GOAL方向を上下左右へ多様化。
- 固定ブロックの盤面端・無意味位置を抑制。
- 初期配置を複数候補から接続最小化する方式へ変更。
- 採用済み生成シードを周回・LEVEL単位で固定。
- LEVEL 22の再訪交差確定を削除。
- 再訪交差の出現カーブを段階化。
- T字ダミー枝を難易度に応じて長く生成。
- 再訪生成失敗時にT字へ難易度を振り替えるフォールバックを追加。
- LEVEL 31以降の「T字0・立体交差0」を監査NG化。
- LEVEL 91–100で有効立体交差最低1個を要求。
- デバッグ正解ルート表示をステージ跨ぎで維持。
- デバッグにダミールート表示を追加。
- タイトル画面、専用GAME START SE、BGM起動制御を追加・調整。
- 図鑑を固定LEVELではなく実際の初登場部品判定へ変更。
- `safeStorage` によりローカルHTML直開きの保存制限へ耐性を追加。

---

## 37. 仕様要約

Electric Linkの現行生成で最も重要な考え方は、

> **「特殊ギミックを置く」こと自体を目的にせず、論理的に成立した回路を作ったうえで、プレイヤーが迷う余地を設計する。**

という点です。

さらにVer.1.5.04では、

> **「品質条件を守るため特殊ギミックを減らした結果、後半ステージが簡単になりすぎる」ことも避ける。**

という難易度下限の考え方を追加しています。

つまり、

```text
再訪交差が成立する
  → 再訪を使った難しさ

再訪交差が成立しない
  → T字へ難易度を振り替える

どちらも成立しない
  → その候補は採用しない
```

という構成です。

T字・ライト番号・立体交差はゲームルールそのものに影響するため、今後も **「正しいトポロジーを生成 → 厳格に監査 → 正常候補だけ採用」** を基本方針とします。
